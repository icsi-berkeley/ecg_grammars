// X-schema / x-net / aspect grammar can go here (and other files).
// It can then be imported into other grammars for specific
// domain applications.

package xnet
import starter

schema ControllerProcess
	subcase of Process
	evokes SPG as anchor
	roles 
	  begin
	  end
	constraints
	  begin <--> anchor.source
	  end <--> anchor.goal
	  

	  